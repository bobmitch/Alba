<?php
// All puck_api responses are emitted from model.php and exit via die(). This
// view should never render — it exists so the Controller dispatch is satisfied.
?>
<!-- puck_api: model exited without responding -->
